---
const {href, theme_title, auth1, title1, auth2, title2, auth3, title3} = Astro.props;
---

<style>
  .feature-card2__card {
    background-color: var(--theme-surface-1);
    color: var(--theme-on-surface-1);
    border-radius: var(--theme-shape-radius);
  }
  .feature-card2__link {
    display: block;
    text-decoration: none;
  }
  .featured-card2__content {
    padding: 0.5rem 0.5rem 0.5rem 0.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    overflow-y: hidden;
  }
  .feature-card2__title {
    font-family: var(--theme-font-family-sans);
  }
</style>


<div class="feature-card2__card">
  <a class="feature-card2__link" href={href}>
    <div class="featured-card2__content">
      <h3 style="text-align:center;" class="feature-card2__title">{theme_title}</h3>
      <ul>
      <li><em>{auth1}</em>: {title1}</li>
      <li><em>{auth2}</em>: {title2}</li>
      </ul>
    </div>
  </a>
</div>